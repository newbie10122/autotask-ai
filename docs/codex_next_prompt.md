# Next Codex Prompt

Use `docs/CODEX_HARNESS_PROMPT.md` as the governing harness prompt.

## Immediate run objective

Complete the next Milestone 0 vertical slice:

1. Reconcile the active roadmap branch and obsolete PR #1.
2. Add repository CI that runs the broadest safe existing test suite plus import/compile, migration, secret/repository hygiene, read-only enforcement, redaction, prompt-injection, authorization/client-isolation, and redacted Compose validation where currently supported.
3. Add a capability certification and three-run Quality Streak evidence format without falsely marking existing capabilities certified.
4. In parallel, delegate read-only/test-first preparation for:
   - authentication and RBAC;
   - client/company scope and cross-client negative cases;
   - prompt-injection scanning and independent answer verification.
5. Serialize any overlapping schema/API changes after coordinator review.
6. Update implementation status, acceptance criteria, known risks, next prompt, and the sanitized Second Brain projection.
7. Continue to the next eligible safe slice without routine user questions or notifications.

Do not perform production deployment, access secrets, expand customer-data scope, make irreversible migrations, or implement any Autotask write capability.
